interface:
  display_name: "MoneyGame Blog Posting"
  short_description: "책 기반 블로그 구조화"
  default_prompt: "Use $moneygame-blog-posting to plan or draft book-based blog posts under Additionals/blog_posts/. Apply the cross-format content shape rules so each post separates reader problem, core message, proof/example, and action CTA; create and embed 2 images when available."

policy:
  allow_implicit_invocation: false
